{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions - frontend uses HTTP API only, no Tauri IPC commands needed",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
